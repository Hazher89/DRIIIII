/// Tekst som endres ved hver publisering — bruk den for å verifisere at nettleseren har ny build
/// (Cloudflare / CDN kan cache gammel `main.dart.js`).
class BuildInfo {
  static const String clientTag = 'driftpro-web 2026.06.07-survey-v6';
}
