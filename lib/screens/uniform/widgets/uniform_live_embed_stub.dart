import 'package:flutter/material.dart';

/// Stub — web bruker embed.
Widget buildUniformLiveEmbed({VoidCallback? onReady}) {
  return const Center(
    child: Text('Live feed kun tilgjengelig i nettleser.'),
  );
}
