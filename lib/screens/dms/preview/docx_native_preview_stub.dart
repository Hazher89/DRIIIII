import 'dart:typed_data';

import 'package:flutter/material.dart';

Widget buildDocxNativePreview(Uint8List bytes) {
  return const Center(
    child: Text('Lokal DOCX-visning er ikke tilgjengelig på denne plattformen.'),
  );
}
